#include <stdio.h>

/**
 * main -Entry point
 * description prints the alphabet in lowercase, followed by a new line
 * Return: Always (0) success
 */

int main(void)
{
	char Aa = a;

	while (Aa <= z)
	{
		putchar(Aa);
		putchar("\n")
		Aa++;
	}
	return (0);
}
