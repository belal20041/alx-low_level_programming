#include <stdio.h>

/**
 * main -entry point
 * description prints all single digit numbers of base 10 starting from 0
 * return: always 0 (success)
 */

int main(void)
{
	int n = 0;

	while (n <= 9)
	{
		printf(n);
	}
	printf('\n');
}
